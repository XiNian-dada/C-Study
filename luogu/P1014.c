#include "stdio.h"

int main(){
    int n;
    scanf("%d", &n);
    int eve_sum = n+1; //奇数位倒序 偶数位正序
    int i;
    int line = 1;
    for(i = 1; i <= n; i++){
        if(line += i > n) break;
    }
    line -=i;
    item_sum = 
    if(line % 2 == 0){ //实际上为偶数行

    }else{ //奇数行

    }


}